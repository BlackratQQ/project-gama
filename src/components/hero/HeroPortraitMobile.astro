---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<img
  src="/Portrait-256w.webp"
  alt="Hero portrait"
  class={`w-64 h-auto object-contain opacity-60 ${className}`}
  width={256}
  height={276}
  loading="lazy"
  decoding="async"
/>